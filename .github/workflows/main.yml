name: CI - CD
on:
  push:
    branches: [ "init" ] # Jika sebelumnya memakai `init`, ganti ke `main`

jobs:
  run-composer-and-docker:
    name: Continuous integration & Continuous Deployment
    runs-on: ubuntu-latest
    steps:

      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Setup PHP with composer v2
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          tools: composer:v2

      - name: Cache Composer packages
        id: composer-cache
        uses: actions/cache@v3
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-php-

      - name: Running composer install
        run: composer install

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v3
        with:
          context: .
          file: "./.docker/php/Dockerfile"
          push: true
          tags: dziljalal/dockify-php:latest
          cache-from: type=registry,ref=dziljalal/dockify-php:latest
          cache-to: type=inline

#      - name: Remote SSH DigitalOcean
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.SSH_HOST }}
#          username: ${{ secrets.SSH_USERNAME }}
#          key: ${{ secrets.SSH_KEY }}
#          script: |
#            cd dockify
#            # Use production compose file
#            docker compose -f docker-compose.yml pull
#            docker compose -f docker-compose.yml down
#            docker compose -f docker-compose.yml up -d --wait
#
#            # Wait for DB readiness (prefer healthcheck, fallback to mysqladmin ping)
#            MAX_WAIT=300   # seconds total max
#            INTERVAL=2     # seconds between checks
#            elapsed=0
#
#            echo "Detecting DB container..."
#            DB_CID=$(docker compose -f docker-compose.yml ps -q db || true)
#            if [ -z "$DB_CID" ]; then
#              echo "ERROR: db service container not found (docker compose ps -q db returned empty)."
#              docker compose -f docker-compose.yml ps || true
#              exit 1
#            fi
#
#            echo "DB container id: $DB_CID"
#
#            # Check whether health info exists
#            HAS_HEALTH=$(docker inspect -f '{{json .State.Health}}' "$DB_CID" 2>/dev/null || true)
#
#            if [ "$HAS_HEALTH" != "null" ] && [ -n "$HAS_HEALTH" ]; then
#              echo "Healthcheck present, waiting for 'healthy' status..."
#              while true; do
#                status=$(docker inspect -f '{{.State.Health.Status}}' "$DB_CID" 2>/dev/null || true)
#                echo "DB health status: ${status} (elapsed ${elapsed}s)"
#                if [ "$status" = "healthy" ]; then
#                  echo "DB container is healthy"
#                  break
#                fi
#                if [ "$elapsed" -ge "$MAX_WAIT" ]; then
#                  echo "DB container did not become healthy within ${MAX_WAIT}s. Dumping last 300 lines of db logs:"
#                  docker compose -f docker-compose.yml logs --no-color --tail=300 db || true
#                  exit 1
#                fi
#                sleep $INTERVAL
#                elapsed=$((elapsed + INTERVAL))
#              done
#            else
#              echo "No healthcheck configured; falling back to mysqladmin ping"
#              # Fallback: try to ping MySQL from the db container itself (expand password inside container)
#              until docker compose -f docker-compose.yml exec -T db bash -lc 'mysqladmin ping -h 127.0.0.1 -u root -p"$MYSQL_ROOT_PASSWORD" --silent' >/dev/null 2>&1; do
#                if [ "$elapsed" -ge "$MAX_WAIT" ]; then
#                  echo "MySQL did not become ready within ${MAX_WAIT}s. Dumping last 300 lines of db logs for debug:"
#                  docker compose -f docker-compose.yml logs --no-color --tail=300 db || true
#                  exit 1
#                fi
#                echo "mysqladmin ping not ready yet (elapsed ${elapsed}s)..."
#                sleep $INTERVAL
#                elapsed=$((elapsed + INTERVAL))
#              done
#            fi
#
#            echo "DB ready after ${elapsed}s. Running Yii migrations..."
#            docker compose -f docker-compose.yml exec -T php /var/www/html/yii migrate --interactive=0
